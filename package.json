{
  "name": "design-system-mobile",
  "private": true,
  "packageManager": "pnpm@10.15.0",
  "scripts": {
    "check-types": "turbo run check-types",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "build": "turbo run build",
    "build:docs": "mkdocs serve -a localhost:8080",
    "dev": "turbo run dev",
    "dev:storybook": "turbo run dev --filter @equinor/mobile-storybook",
    "lint": "turbo run lint",
    "lint:component": "turbo run lint --filter design-system-mobile-components",
    "clean": "turbo run clean && rm -rf node_modules .turbo .pnpm",
    "clean:storybook": "turbo run clean --filter @equinor/mobile-storybook",
    "syncpack:list": "pnpm syncpack list-mismatches",
    "syncpack:fix": "pnpm syncpack fix-mismatches",
    "syncpack:update": "pnpm syncpack update",
    "syncpack:format": "pnpm syncpack format",
    "docker:build": "docker build -t equinor/oss-web .",
    "docker:start": "docker run -p 8080:8080 equinor/oss-web",
    "docker:inspect": "docker run -it equinor/oss-web /bin/sh"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.39.0",
    "@typescript-eslint/parser": "^8.39.0",
    "eslint": "^9.25.0",
    "prettier": "^3.5.3",
    "syncpack": "^13.0.3",
    "tsup": "^8.5.0",
    "turbo": "^2.5.4",
    "typescript": "~5.8.3"
  },
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "packages/**/*"
  ]
}