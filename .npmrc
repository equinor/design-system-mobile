# Use hoisted node linker for better Metro compatibility
node-linker=hoisted
# Enable shameful hoisting to ensure Metro can find internal modules
shamefully-hoist=true
# Enable public hoisting for better monorepo support
public-hoist-pattern[]=*
# Ensure symlinks are preserved for workspace packages
symlink=true